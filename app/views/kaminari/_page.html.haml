%li{class: "#{'current' if page.current?}"}
  = link_to page, page.current? ? '#' : url, {:remote => remote, class: 'button radius secondary', :rel => page.next? ? 'next' : page.prev? ? 'prev' : nil}
