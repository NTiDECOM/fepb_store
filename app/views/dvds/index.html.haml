%p#notice= notice
%h1 Listing Dvds
%table
  %thead
    %tr
      %th{:colspan => "3"}
  %tbody
    - @dvds.each do |dvd|
      %tr
        %td= link_to 'Show', dvd
        %td= link_to 'Edit', edit_dvd_path(dvd)
        %td= link_to 'Destroy', dvd, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Dvd', new_dvd_path
